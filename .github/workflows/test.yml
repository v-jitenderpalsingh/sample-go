name: Test workflow

on: [push, workflow_dispatch]

jobs:  
  test-v7:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - uses: actions/checkout@v7
      - name: Set up Go
        uses: v-priyagupta108/setup-go@collapse-setup-logs
        with:
          go-version: 1.26
      
      - name: Run main
        run: |
          go version
          go run main.go